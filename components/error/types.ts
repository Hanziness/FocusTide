export enum ErrorActionState {
  Recommended = 'recommended',
  Default = 'default',
  Disabled = 'disabled'
}

export enum ActionType {
  RECOMMEND = 'recommended',
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  HIDE = 'disabled'
}
